name: LCD color trace v5 historical (non-authoritative)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  historical-only:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'The iftu_csc_trace protocol-v5 build is historical only. Gate-0 hardware authority is .github/workflows/gate0-v7-candidate.yml.'
