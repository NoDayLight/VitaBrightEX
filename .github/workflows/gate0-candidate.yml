name: Gate-0 v6 historical (non-authoritative)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  historical-only:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Protocol v6 is retained only as historical research evidence. Gate-0 hardware authority is .github/workflows/gate0-v7-candidate.yml.'
